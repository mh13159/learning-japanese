# Handoff — 2026-05-25 23:06 PST (18:06 UTC)

Snapshot of project state at the end of this work block. Per-session file (`docs/handoff-<YYYY-MM-DD-HHMM>.md`). Regenerated every 30 minutes via `/loop 30m` (see [Loop setup](#loop-setup) below).

## Branch & remote

- **Branch:** `main` at `3faa236` (clean merge of `feat/rtk-token-saving`)
- **Remote:** `origin = https://github.com/mh13159/learning-japanese`
- **Default branch on GitHub:** `main`
- **Open branches:** `docs/add-post-clone-changelog` and `feat/rtk-token-saving` still exist locally and remotely — both fully merged. Safe to delete; left intact pending user decision.

## Recent commit graph

```
3faa236  Merge feat/rtk-token-saving into main           (merge)
├─ 9854c3a  feat: add .claude/skills/ and docs/ for project conventions
├─ ab4a216  feat: install RTK token-saver and document conventions
├─ 494aa0c  docs: add .AI-Agents framework and declare NVIDIA SDK placeholder
├─ ad391c1  docs: add comprehensive post-clone changelog and documentation
4262fe9  Add Japanese translator UI and shadcn components
1ce5101  Initial commit from Create Next App
```

## Uncommitted local changes

| File | Status | Why |
|------|--------|-----|
| `package.json` | M | Removed the bogus `@nvidia-ai/sdk: latest` line so `npm install` would resolve. The line was never a real npm package; commit-worthy. |
| `package-lock.json` | M | Regenerated by Windows-side npm 11.12.1 / Node 24 during the install. Should be committed alongside package.json. |
| `next-env.d.ts` | ?? | Auto-written by `next dev`. Already in Next's standard `.gitignore` template — verify the repo's `.gitignore` covers it before committing. |

**Recommended next commit:**
```
chore: drop placeholder @nvidia-ai/sdk and refresh lockfile
```
Single commit on `main` (or a `chore/fix-package-json` branch per the new-branch-per-task convention).

## Environment

- **Workstation:** Windows 11 Pro, project at `e:\Learning Japanese - v2\japanese-translator`
- **Workspace secrets:** `e:\Learning Japanese - v2\.env.local` (outside the repo, not tracked) holds AI provider keys + `GITHUB_TOKEN` (fine-grained PAT, expires 2026-06-24, `Contents: write` on `mh13159/learning-japanese`)
- **Node runtime used for install:** Windows Node v24.15.0 / npm 11.12.1 (494 packages, 7 min)
- **Why not WSL:** WSL Ubuntu 20.04 ships Node 10; installed Node 20.20.2 via nvm but `npm install` failed on `/mnt/e/` with `EACCES` on `rename` — known WSL+NTFS interop limitation. Switched to Windows-side Node. The dev server itself can still run from either, but install must be Windows-side until the project is moved off `/mnt/e/`.

## Token-saving stack

- **RTK v0.42.0** installed at `C:\Users\Muhammad Hamza\bin\rtk.exe`. Global PreToolUse hook in `~/.claude/settings.json` rewrites Bash commands transparently. Project-local filters template at `.rtk/filters.toml`.
- **CLAUDE.md** root file imports `@AGENTS.md` + `@.claude/CLAUDE.md` and embeds the RTK command reference.
- **`.claude/CLAUDE.md`** documents project-level conventions: parallel tool calls, targeted reads, Anthropic prompt caching, Next.js `unstable_cache` patterns.
- **Skills directory** `.claude/skills/` has 4 custom (`token-audit`, `new-task-branch`, `wire-provider`, `docs`) plus 23 imported general-purpose skills.

## Dev server

- Started in background via `rtk npm run dev` from project root.
- Background task ID: `b2r57rhvm` (log at `/tmp/next-dev.log`).
- Expected URL: http://localhost:3000
- **Status at handoff time:** starting up — Next.js 16 cold compile in progress. Smoke test still pending.

## Known gaps the next session should address

1. **Translator is a UI mock.** `components/translator.tsx:15-19` returns hardcoded strings; no real provider call. The `wire-provider` skill in `.claude/skills/` codifies the wire-up path.
2. **`.AI-Agents/` is markdown-only.** No orchestrator code, no provider clients, no API routes. README and CHANGELOG describe an agentic framework that doesn't exist yet.
3. **`package.json` removal of `@nvidia-ai/sdk: latest` is uncommitted.** First action of the next session if not handled here.
4. **No tests.** No Jest/Vitest/Playwright config. Adding the first test would also exercise the `test-driven-development` skill.
5. **Default metadata.** `app/layout.tsx` still has `"Create Next App"` as title/description.

## Loop setup

To regenerate this file every 30 minutes while Claude Code is open, invoke:

```
/loop 30m write a fresh handoff doc at docs/handoff-<YYYY-MM-DD-HHMM>.md following the format of docs/handoff-2026-05-25-2306.md, using the current timestamp and the latest git state
```

This uses the `loop` skill registered in your session. It runs in-conversation, so it only fires while Claude Code is active. For handoffs that fire even when no session is open, the `schedule` skill creates a remote cron-style routine — but that needs the cloud-agent infra enabled on the account.

## Where to look

- Project root: [`japanese-translator/`](..)
- Conventions: [`CLAUDE.md`](../CLAUDE.md), [`.claude/CLAUDE.md`](../.claude/CLAUDE.md), [`CONTRIBUTING.md`](../CONTRIBUTING.md)
- AI orchestration design notes: [`.AI-Agents/`](../.AI-Agents/)
- 4-way translator design: [`docs/japanese_4way_translator_handoff.md`](japanese_4way_translator_handoff.md), [`docs/v1japanese_4way_translator_handoff.md`](v1japanese_4way_translator_handoff.md)
